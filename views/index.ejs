<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IT Tech Club</title>

    <!-- 1️⃣ Bootstrap FIRST -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- 2️⃣ Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">

    <!-- 3️⃣ AOS CSS -->
    <link href="https://unpkg.com/aos@2.3.4/dist/aos.css" rel="stylesheet">

    <!-- 4️⃣ Custom CSS LAST -->
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>

<%- include('partials/navbar') %>

<!-- ================= HERO SECTION ================= -->
<section class="hero d-flex align-items-center justify-content-center text-center">
    <div class="hero-overlay">
        <div data-aos="fade-up">
            <h1 class="hero-title">Welcome to IT Tech Club</h1>
            <p class="hero-subtitle">Innovate • Learn • Build 🚀</p>
            <a href="#about" class="btn btn-info mt-3">Explore More</a>
        </div>
    </div>
</section>

<!-- ================= ABOUT SECTION ================= -->
<section id="about" class="py-5 bg-dark text-white">
    <div class="container text-center" data-aos="fade-right">
        <h2 class="mb-4">About Us</h2>
        <p class="lead">
            IT Tech Club is a community of passionate developers, innovators,
            and tech enthusiasts. We organize workshops, hackathons,
            coding competitions and technical events.
        </p>
    </div>
</section>

<!-- ================= EVENTS SECTION ================= -->
<section id="events" class="py-5 bg-light">
    <div class="container">
        <h2 class="text-center mb-5" data-aos="zoom-in">
            Upcoming Events
        </h2>

        <div class="row">

            <% if (events && events.length > 0) { %>

                <% events.forEach(event => { %>
                    <div class="col-md-4 mb-4" data-aos="fade-up">
                        <div class="card shadow h-100">
                            <div class="card-body text-center">
                                <h5 class="card-title"><%= event.event_name %></h5>
                                <p class="card-text"><%= event.description %></p>
                                <p class="text-muted">
                                    <%= new Date(event.date).toDateString() %>
                                </p>
                                <a href="/events" class="btn btn-primary btn-sm">
                                    View Details
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>

            <% } else { %>

                <div class="col-12 text-center">
                    <p>No upcoming events available.</p>
                </div>

            <% } %>

        </div>
    </div>
</section>

<%- include('partials/footer') %>

<!-- ================= JS ================= -->

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<!-- AOS -->
<script src="https://unpkg.com/aos@2.3.4/dist/aos.js"></script>
<script>
    AOS.init({
        duration: 1000,
        once: true
    });
</script>

</body>
</html>